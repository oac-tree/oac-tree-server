#  SUP_AUTO_SERVER_INCLUDE_DIRS - include directories
#  SUP_AUTO_SERVER_LIBRARIES    - libraries to link against

get_filename_component(SUP_AUTO_SERVER_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

if(TARGET sup-auto-server::sup-auto-server)
else()
  message(STATUS "sup-auto-server-config.cmake including ${SUP_AUTO_SERVER_CMAKE_DIR}/sup-auto-server-targets.cmake")
  include("${SUP_AUTO_SERVER_CMAKE_DIR}/sup-auto-server-targets.cmake")
endif()

set(SUP_AUTO_SERVER_LIBRARIES sup-auto-server::sup-auto-server)

message(STATUS "sup-auto-server-config.cmake in ${CMAKE_CURRENT_LIST_FILE}")
message(STATUS "SUP_AUTO_SERVER_LIBRARIES ${SUP_AUTO_SERVER_LIBRARIES}")
